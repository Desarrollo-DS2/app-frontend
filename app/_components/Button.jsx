import React from 'react'

import { Button } from 'antd'

function Button(props) {
  const { text, type, htmlType, onClick } = props

  return (
    <Button type="primary" htmlType="submit" co>
      text
    </Button>
  )
}
